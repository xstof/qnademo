name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: 'Build-Quasar-Frontend'
    steps:
      # To use this repository's private action, you must check out the repository
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build-Quasar
        uses: ./gh-action-build-frontend # Uses an action in the root directory
        id: build-quasar
        with:
          sourcelocation: $(GITHUB_WORKSPACE)/qnademo/qna
      # Use the output from the `hello` step
